## 매트릭스 사용하기 ## 
# job 하나에 OS는 하나만 붙는다
name: 'SWM GitHub Actions Basic'

on: ['push', 'pull_request']
# on:
#   push:
#     branches:
#       - 'main'
#     paths:
#       - 'src/**'
#       - 'test/**'
jobs:
  first-job:
    name: 'First Job'
    runs-on: ubuntu-latest

    steps:
    - name: Hello
      shell: bash
      run: |
        echo "hello world"       
  second-job:
    name: 'Second Job'
    needs:
    - 'first-job'
    # - 'second-job'
    runs-on: ubuntu-latest

    steps:
    - name: Hello
      shell: bash
      run: |
        echo "hello world"
          